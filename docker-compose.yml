# Define the services (containers) that make up your app.
services:
  music-bot:
    # Build the Docker image from the Dockerfile in the current directory ('.')
    build: .
    # Name the container for easier identification.
    container_name: discord-music-bot
    # Restart the bot automatically if it crashes, but not if you manually stop it.
    restart: unless-stopped
    # Load environment variables from the .env file.
    # This securely passes your BOT_TOKEN to the container without hardcoding it.
    env_file:
      - .env
    # Optional: Mount a volume for logs and cookies.
    # Using a named volume 'logs_data' to avoid permission issues with host bind mounts.
    volumes:
      - logs_data:/app/logs
      - ./cookies.txt:/app/cookies.txt:ro
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis

  redis:
    image: redis:alpine
    container_name: music-bot-redis
    restart: always
    volumes:
      - redis_data:/data

volumes:
  redis_data:
  logs_data: